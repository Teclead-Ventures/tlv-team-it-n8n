# Copy this file to deploy/.env and adjust values

# General
GENERIC_TIMEZONE=Europe/Berlin

# n8n networking
N8N_PROTOCOL=http
N8N_HOST=localhost
N8N_PORT=5678
WEBHOOK_URL=http://localhost:5678/

# Security / telemetry
N8N_PUBLIC_API_SWAGGERUI_DISABLED=true
N8N_METRICS=false
QUEUE_HEALTH_CHECK_ACTIVE=true

# Importer (Public API)
# After creating the owner user in n8n, create an API key and paste here
N8N_API_KEY=

# Optional: provision credentials via overwrite file (mount the file and set path)
# CREDENTIALS_OVERWRITE_FILE=/app/deploy/credentials-overwrite.json

